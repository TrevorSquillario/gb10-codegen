model: deepseek-ai/DeepSeek-V4-Flash
container: scitrera/dgx-spark-vllm-jasl-ds4:20260609
defaults:
  port: 8018
  host: 0.0.0.0
  tensor_parallel: 2
  gpu_memory_utilization: 0.825
  max_model_len: 1048576
  max_num_batched_tokens: 8192
  max_num_seqs: 2
  tool_call_parser: deepseek_v4
  pipeline_parallel: 1
  data_parallel: 1
  max_cudagraph_capture_size: 8
  block_size: 256
  load_format: instanttensor
  kv_cache_dtype: fp8
  reasoning_parser: deepseek_v4
  tokenizer_mode: deepseek_v4
  speculative_mtp: '{"method":"mtp","num_speculative_tokens":2}'
  compilation_config: '{"cudagraph_mode":"FULL_AND_PIECEWISE","custom_ops":["all"]}'
env:
  TILELANG_CLEANUP_TEMP_FILES: '1'
  VLLM_TRITON_MLA_SPARSE: '1'
  VLLM_TRITON_MLA_SPARSE_HEAD_BLOCK_SIZE: '4'
  VLLM_TRITON_MLA_SPARSE_QUERY_CHUNK_SIZE: '512'
command: |
  vllm serve {model} \
    --served-model-name {model} agentev3 agente \
    --host {host} \
    --port {port} \
    --max-model-len {max_model_len} \
    --max-num-seqs {max_num_seqs} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --trust-remote-code \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --enable-auto-tool-choice \
    --tool-call-parser {tool_call_parser} \
    --tokenizer-mode {tokenizer_mode} \
    --reasoning-parser {reasoning_parser} \
    --kv-cache-dtype {kv_cache_dtype} \
    --load-format {load_format} \
    --speculative-config '{speculative_mtp}' \
    --compilation-config '{compilation_config}' \
    --enable-prefix-caching \
    --block-size {block_size} \
    --max-cudagraph-capture-size {max_cudagraph_capture_size} \
    -tp {tensor_parallel} \
    -pp {pipeline_parallel}
runtime: vllm
min_nodes: 2
metadata:
  description: deepseek v4 flash 1M de contexto
  maintainer: www.codigo6.com <omar@codigo6.com>
  model_dtype: fp8
  num_layers: 43
  num_kv_heads: 1
  head_dim: 512
  quantization: fp8
  quant_bits: 8
  kv_dtype: fp8
recipe_version: '2'